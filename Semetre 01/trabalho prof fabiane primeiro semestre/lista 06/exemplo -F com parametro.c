#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <math.h>
#include "l"
int main()
{
    float x, y;
    printf("\n Funcao principal");
    printf("\n Digite o valor de X: ");
    scanf("%f", &x);
    printf(" \n Digite o valor de Y: ");
    scanf("%f", &y);
    soma (x,y);
    printf("\n a soma e: %f",soma);
    return 0;
}